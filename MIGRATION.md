# پایگاه داده

برای نصب نسخه اولیه، فقط فایل زیر را اجرا کنید:

`Database/001_initial.sql`

این فایل اکنون **نسخه کامل و نهایی ساختار اولیه دیتابیس** است و شامل تمام جدول‌ها، کلیدهای خارجی، ایندکس‌ها، Precision امتیازها و محدودیت‌های داده موردنیاز نسخه فعلی سامانه است.

فایل Upgrade جداگانه برای نصب جدید موردنیاز نیست. چون این محیط آزمایشی هنوز داده عملیاتی ندارد، در صورت تغییر ساختار عمده در آینده، نسخه جدید اسکریپت ساخت اولیه به‌صورت یکپارچه منتشر خواهد شد.

پس از اجرای اسکریپت، برنامه را اجرا کنید؛ برنامه فقط وجود ساختار پایگاه داده را کنترل می‌کند و اسکریپت ارتقا را خودکار اجرا نمی‌کند.